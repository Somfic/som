struct shape:
	shape_type ~ shape_type
	width ~ number
	height ~ number

enum shape_type:
	square
	rectangle
	circle

trait area:
	fn area() -> number

impl area for shape:
	fn area() -> number:
		width * height;
